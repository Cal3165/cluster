#- op: remove
#  path: /cluster/apiServer/admissionControl
cluster:
  apiServer:
    admissionControl:
      - configuration:
          defaults:
            audit: restricted
            audit-version: latest
            enforce: privileged
            enforce-version: latest
            warn: restricted
            warn-version: latest
        name: PodSecurity
